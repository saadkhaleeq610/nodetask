import getPosts, {getPostsLength} from "./postController.js";

// const {generateRandomNumber, celciusToFarenhiet} = require('./utils');

// console.log(`Random no: ${generateRandomNumber()}`);
// console.log(`Temp: ${celciusToFarenhiet(0)}`);

console.log(getPosts());
console.log(getPostsLength());